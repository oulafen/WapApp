<%= form_for(@reply) do |f| %>
  <% if @reply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reply.errors.count, "error") %> prohibited this reply from being saved:</h2>

      <ul>
      <% @reply.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :replier %><br>
    <%= f.number_field :replier %>
  </div>
  <div class="field">
    <%= f.label :topic %><br>
    <%= f.number_field :topic %>
  </div>
  <div class="field">
    <%= f.label :reply_body %><br>
    <%= f.text_area :reply_body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
